@model AKCore.Models.MenuEditModel

<div id="addSubMenuModal" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title">Lägg till undermeny</h4>
            </div>
            <form action="/MenuEdit/AddSubMenu" id="add-submenu-form" method="post">
                <div class="modal-body">
                    <div class="alert alert-success" style="display: none;">
                    </div>
                    <div class="alert alert-danger" style="display: none;">
                    </div>
                    <input type="hidden" name="parentId" class="parentId" />
                    <div class="form-group">
                        <label>Namn</label>
                        <input type="text" class="form-control" name="text" placeholder="Namn" required>
                    </div>
                    <div class="form-group">
                        <select name="pageId" class="form-control chosen-select" placeholder="Sida" required>
                            <option value="">Välj en sida</option>
                            @foreach (var p in Model.Pages)
                            {
                                <option value="@p.Id">@p.Name</option>
                            }
                        </select>
                    </div>
                   

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Stäng</button>
                    <button type="submit" class="btn btn-primary">Skapa</button>
                </div>
            </form>

        </div>
    </div>
</div>