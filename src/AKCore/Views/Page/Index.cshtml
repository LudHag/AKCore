@model AKCore.Models.PageRenderModel
@{
    Layout = "~/Views/Shared/_MasterLayout.cshtml";
}
<script>
    var widgetAlbums = {};
</script>

@foreach (var w in Model.Widgets)
{
    @Html.Partial("Widgets/" + w.Type, w);
}