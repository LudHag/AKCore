@model AKCore.DataModel.Widget
<div class="row">
    <div class="col-sm-12">
        <ul id="youtubelist">
            @if (Model.Videos != null)
            {
                foreach (var v in Model.Videos)
                {
                    <li><a href="https://www.youtube.com/watch?v=@v" data-name="hej"></a></li>
                }
            }
        </ul>
    </div>
</div>
